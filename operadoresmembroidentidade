#operador de membro
lista = [1, 2, 3, "ana", "carla"]
2 in lista
print(2 in lista)
print('ana' not in lista)

#operador de identidade
x = 3
y = x
z = 3
print(x is z)
print(y is z)
print(x is not z)

lista_a = [1, 2, 3]
lista_b = lista_a
lista_c = [1, 2, 3]
print(lista_a is lista_b)  #mesmo comportamento das variaveis
print(lista_b is lista_c)
print(lista_a is not lista_c)
#embora tenham o mesmo conteudo, lista_a e lista_c sao diferentes
#lista_a e lista_b apontam para o mesmo espaco de memoria, o que indica que elas de fato são inguais. 
#já a lista_c aponta para outro espaco de memoria
